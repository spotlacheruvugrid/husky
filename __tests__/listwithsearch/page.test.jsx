const { render } = require('@testing-library/react');
import SearchComp from '@//app/listwithsearch/page';

describe('test list is rendered or not', () => {
  it('test', async () => {
    expect(1).toBe(2);
  });
});
